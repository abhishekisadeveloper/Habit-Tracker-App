import { colors } from "./colors";
import { typography } from "./typography";
import { spacing } from "./spacing";
import { ThemeProvider, useTheme } from "./useTheme";

export const theme = {
  colors,
  typography,
  spacing,
};

export { colors, typography, spacing, ThemeProvider, useTheme };